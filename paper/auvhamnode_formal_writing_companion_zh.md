# AUVHamNODE 中文长论文正式写作指导文件：专家复核与主线深化版

> 版本：2026-05-20
> 文件定位：本文件是对 `auvhamnode_paper_writing_guide_expert_revised_zh.md` 的**配套写作指导文件**。已有写作指南更像“完整论文说明书”；本文件更像“正式开写前的专家复核意见 + 叙事主线 + 证据链设计 + 章节执行纲领”。  
> 使用方式：建议先读已有写作指南确定术语、章节和禁用表述，再用本文件统一全文论证逻辑、摘要/引言主线、方法章节组织方式、实验证据阶梯和审稿风险控制。
> 2026-05-20 更新：当前正式主稿采用正文骨架 v2，主标题不以 `AUVHamNODE` 为中心；`AUVHamNODE` 在正文中作为方法简称使用。

---

## 0. 专家复核结论

经对附件中的 `AUVHamNODE.py`、`auv_model_registry.py`、`auv_baselines.py`、`data_collection.py`、`remus100_core.py`、`train_auv_hamnode.py`、`train_utils.py`、`rollout_benchmark_engine.py`、`rollout_benchmark_reporting.py`、`evaluate_rollout_benchmark.py` 以及已有中文写作指南进行复核，上一轮分析的核心判断**成立**，没有发现会影响论文主线的实质性错误。

但为了正式写作，需要把若干表述进一步收紧。最重要的一点是：

> **AUVHamNODE 不应被写成“完整闭合严格端口哈密顿 AUV 系统”，而应被写成“面向六自由度 AUV 动力学学习的 SE(3) 结构化、能量约束、开放式端口哈密顿风格机械核心的神经常微分方程模型”。**

这一定位与当前实现高度一致：模型在相对水速度坐标中构造机械核心，使用正定逆质量、标量势能、正定阻尼、斜对称 lift、执行器广义力分支和一阶执行器滞后；增强状态包含位置、旋转矩阵、相对速度、执行器实际状态、命令、可选海流和可选深度上下文；有海流时，位置运动学使用总体速度，水动力相关分支使用相对水速度。

本文件确认上一轮分析中的以下核心思路可以正式采用：

1. 用“几何账本、能量账本、速度账本”统一全文；
2. 将 AUVHamNODE 的贡献写成“结构化动力学学习框架”，而不是单一网络结构；
3. 将相对水速度约定作为全文地基；
4. 将实验写成“证据阶梯”，而不是简单堆 RMSE 表格；
5. 主动设置“理论声称范围表”和“审稿风险控制表”；
6. 将长期 rollout、失败率、能量诊断和 SO(3) 诊断作为论文可信度的关键证据。

---

## 1. 与已有写作指南的关系

已有写作指南已经提供了完整的章节结构、术语表、理论推导框架、模型/基线/消融清单、实验矩阵和禁用表述。本文件不替代它，而是补充三个方面。

第一，已有指南偏“全面规范”，本文件偏“正式写作时的主线决策”。也就是说，已有指南告诉你论文应该包含什么，本文件告诉你**为什么这些内容应按这个顺序出现**。

第二，已有指南已经强调理论边界，本文件进一步把这些边界转换为写作时可执行的判断：哪些话可以放进摘要，哪些话只能放进方法章，哪些话必须放进讨论或局限性。

第三，本文件把上一轮分析中的“三个账本”“证据阶梯”“审稿风险控制表”“图表叙事设计”系统化，使它们能直接指导正式中文长论文写作。

建议配合方式如下：

| 写作任务 | 优先参考文件 | 本文件的作用 |
|---|---|---|
| 统一术语、模型名、消融名 | 已有写作指南 | 本文件仅强调高风险术语 |
| 写摘要、引言、贡献 | 本文件 | 用“三个账本”和“结构化学习框架”统一叙事 |
| 写理论方法章 | 已有写作指南 + 本文件 | 已有指南给公式，本文件给组织逻辑 |
| 写实验章 | 已有写作指南 + 本文件 | 本文件给证据阶梯和结果解释顺序 |
| 写讨论和局限性 | 本文件 | 用审稿风险控制表主动化解质疑 |
| 准备答辩或审稿回复 | 本文件 | 使用“可写/不可写”边界表 |

---

## 2. 严格复核表：上一轮分析是否成立

| 上一轮分析中的判断 | 复核结论 | 正式写作处理 |
|---|---|---|
| AUVHamNODE 应定位为开放式端口哈密顿风格机械核心，而非完整严格端口哈密顿系统 | 正确 | 摘要、引言和方法章均应使用“开放式”“风格”“机械核心”等限定语 |
| 相对水速度与总体速度关系是方法成立的必要状态约定 | 正确 | 在问题定义章单独说明，作为海流条件下的变量关系和工程一致性处理 |
| 数据保存总体速度，模型内部有海流时使用相对速度 | 正确 | 使用“数据态—模型态转换”表述 |
| 位姿运动学由总体速度推进，水动力/广义力分支由相对速度参数化 | 正确 | 这是关键物理解释之一，应画图说明，但不单独提升为主要创新 |
| 模型包含正定质量、标量势能、正定阻尼、斜对称 lift、可学习广义力和执行器滞后 | 正确 | 方法章按这些结构组织，而不是按代码变量顺序组织 |
| 基线/消融链条支持“结构逐级增强”的论证 | 正确 | 实验章必须分组报告，而不是把所有模型放在一个无解释的大表中 |
| 长期 rollout 和失败率比短时误差更能证明结构价值 | 正确 | 实验章的核心图表应围绕长期递推稳定性展开 |
| SO(3) 诊断和能量诊断应作为内部物理一致性证据 | 正确 | 注意区分“主 KPI”和“内部诊断”，不要把内部诊断写成直接观测误差 |
| 图表应包含速度变量关系图、能量流图、rollout 误差增长图、失败原因图 | 正确 | 建议作为正式论文的核心叙事图 |

需要进一步精细化的表述如下：

| 高风险说法 | 风险 | 推荐改写 |
|---|---|---|
| “AUVHamNODE 是严格端口哈密顿系统” | 过强；执行器、海流、神经广义力分支并非闭合标准端口哈密顿系统 | “AUVHamNODE 构造了开放式端口哈密顿风格机械核心” |
| “模型严格保持 SO(3)” | 普通 ODE 求解器不等于李群积分器 | “连续时间向量场与 SO(3) 切空间结构相容，并通过 SO(3) 正则与诊断监控数值漂移” |
| “D/J/B 分解恢复真实水动力项” | 可辨识性过强 | “D/J/B 分解提供物理诱导的非保守力结构化假设空间” |
| “海流被完整建模为环境哈密顿子系统” | 当前实现中海流是携带外源通道 | “海流作为短积分窗口内的外源上下文，用于构造相对水速度和可选特征” |
| “具体平台模型被端口哈密顿化” | 与实现不符，也容易把数据来源误写成方法贡献 | “具体平台或仿真数据提供受控验证环境，方法贡献是结构化神经动力学建模” |
| “exact SE(3) 表示数值积分严格在 SE(3) 上” | 容易被审稿人抓住 | “显式编码 SE(3) 运动学形式；数值积分后的原始旋转矩阵仍需 SO(3) 诊断” |

---

## 3. 全文应围绕“三个账本”展开

正式论文最容易写散。建议把所有章节都服务于以下三个“账本”。

### 3.1 几何账本：谁负责姿态和位姿一致性

AUV 的配置不是普通欧氏向量，而是：

\[
q=(x,R),\qquad x\in\mathbb{R}^3,\quad R\in SO(3).
\]

普通黑箱模型若直接学习完整状态导数，容易把旋转矩阵当成普通 9 维向量，长期递推后可能出现正交性和行列式漂移。AUVHamNODE 的优势在于：它显式使用

\[
\dot{x}=R v_b,\qquad \dot R=R\hat\omega,
\]

其中有海流时

\[
v_b=v_r+R^\top v_c^n.
\]

正式写作时要强调：

> AUVHamNODE 并不是依赖网络自由学习位姿运动学，而是把位姿运动学作为结构写进连续时间向量场中。

但也必须强调：

> 当前实现使用普通 ODE 求解器时，不能声称数值解严格保持在 SO(3) 上；因此论文需要同时报告 SO(3) 正则和 SO(3) 诊断指标。

### 3.2 能量账本：谁储能、谁耗能、谁交换功率

AUVHamNODE 的机械存储函数建议写成：

\[
H_\theta(q,p_r)=\frac12 p_r^\top M_\theta^{-1}p_r+V_\theta(q),
\qquad p_r=M_\theta \nu_r.
\]

其中：

- \(M_\theta\) 对应正定质量矩阵；
- \(V_\theta(q)\) 对应标量势能；
- \(D_\theta\) 对应耗散；
- \(J_\theta\) 对应斜对称零功率耦合；
- \(\tau_\theta\) 对应执行器与其他可学习非保守广义力。

非保守力可以组织为：

\[
f^{nc}_\theta(\xi)=
-D_\theta(\xi)\nu_r
+J_\theta(\xi)\nu_r
+\tau_\theta(\nu_r,u_a,c).
\]

由于 \(J_\theta=-J_\theta^\top\)，有：

\[
\nu_r^\top J_\theta \nu_r=0.
\]

因此在适当假设下，可写出机械核心的能量平衡：

\[
\dot H_\theta
= -\nu_r^\top D_\theta \nu_r
+\nu_r^\top\tau_\theta
+\text{可能的海流/外源相关项}.
\]

正式论文中建议把这一节写成“能量流图 + 命题 + 证明要点 + 声称边界”。不要把它写成“完整系统严格被动”的证明。

### 3.3 速度账本：数据空间和模型空间如何互相转换

这是全文最关键、最容易被忽略的部分。

数据保存体坐标系总体速度：

\[
\nu_b=[v_b,\omega].
\]

当存在惯性系海流 \(v_c^n\) 时，体坐标系海流为：

\[
v_c^b=R^\top v_c^n.
\]

模型内部使用相对水速度：

\[
\nu_r=
\nu_b-
\begin{bmatrix}R^\top v_c^n\\0\end{bmatrix}.
\]

预测输出回到数据空间时：

\[
\nu_b=
\nu_r+
\begin{bmatrix}R^\top v_c^n\\0\end{bmatrix}.
\]

正式写作必须反复强调：

> **位置运动学需要总体速度，因为位置是相对于惯性系变化；水动力建模需要相对水速度，因为水动力主要取决于航行器相对于水体的运动。**

这也是 AUVHamNODE 相比普通黑箱 NODE 或普通 SE(3) NODE 的一个关键贡献点。

---

## 4. 摘要和引言的推荐主线

### 4.1 摘要先写任务和缺口，不直接堆技术部件

不建议摘要第一句写：

> 本文提出一种端口哈密顿 AUV 模型。

更稳妥的开头应先界定任务：

> 自主水下航行器的长期状态预测依赖准确的运动建模。由于水下运动具有强非线性、强耦合和受控开放系统特征，传统参数化模型和纯黑箱学习模型在精度、可解释性和长期递推稳定性之间仍存在明显张力。

然后再引出方法：

> 为此，本文提出 AUVHamNODE，一种面向长期状态预测的 AUV 结构化神经动力学建模方法。

摘要中可以概括位姿几何、能量与耗散约束、可学习非保守作用和长期递推验证，但不应把海流相对速度、执行器滞后、机械核心、结构消融和实验协议压缩成一串术语清单。摘要的首要任务是让读者明白“研究问题是什么、为什么需要新模型、本文模型解决什么层面的缺口”。

### 4.2 摘要必须明确的三个层次

摘要至少要清楚回答三个问题：

1. **贡献类型**：本文主要提出 AUV 运动建模方法，而不是只提出一个预测器或一个仿真器改写版本。
2. **预测对象**：长期预测指位置、姿态、速度等 AUV 运动状态的长期递推预测。
3. **结构价值**：结构化神经动力学用于在数据驱动建模中保留可靠的几何、能量和耗散约束，同时让难以精确参数化的作用由数据学习。

### 4.3 可直接使用的摘要骨架

> 自主水下航行器的长期状态预测依赖能够刻画受控六自由度运动的动力学模型。传统参数化模型具有清晰的物理结构，但在复杂水动力和平台参数校准方面成本较高；纯数据驱动模型具有较强拟合能力，但在长期递推中容易削弱几何和物理一致性。为此，本文提出 AUVHamNODE，一种面向长期状态预测的 AUV 结构化神经动力学建模方法。该方法在连续时间动力学中显式组织位姿运动几何、机械能量与耗散约束，并用可学习项描述难以精确建模的非保守作用。本文通过短时预测、长期递推、结构消融和物理诊断评估该模型，检验结构约束对 AUV 运动状态预测稳定性和可信度的作用。

这段是摘要骨架，不是最终摘要。最终摘要必须根据实际实验结果调整最后一句，避免在结果未完全校验前写成性能结论。

---

## 5. 引言建议：从研究任务写到方法定位

正式引言建议按以下顺序展开。核心原则是：第 1 节是本章引言，不是后续状态定义、模型构造和验证协议的压缩版。

### 5.1 第一段：AUV 运动建模服务什么任务

第一段应先说明 AUV 运动建模的工程和科学意义，并明确长期状态预测的对象是位置、姿态、速度等运动状态。不要用“附加质量、阻尼、恢复力、执行器、相对来流”等概念清单开头；这些概念可在后文作为传统模型和方法章节的具体内容展开。

### 5.2 第二段：传统参数化模型的价值与局限

传统六自由度模型的优点是能够把坐标、速度、惯性、水动力、恢复力和控制作用放在统一动力学框架中，因而在仿真、控制和工程分析中具有长期价值。其局限不应写成“传统模型过时”，而应写成：复杂水动力、平台参数、环境扰动和实验标定成本使模型在不同任务和工况下仍需大量校准。

### 5.3 第三段：数据驱动模型的价值与局限

数据驱动模型可以降低显式建模成本，直接从轨迹和控制数据中学习状态演化规律。但若将受控 AUV 运动完全视作普通欧氏空间中的黑箱序列映射，长期递推中容易出现几何一致性、物理可解释性和稳定性方面的问题。这里不需要列出全部失败模式，只需要把结构化建模的必要性自然引出。

### 5.4 第四段：结构化神经动力学的启发

这里可以引入神经常微分方程、哈密顿神经网络（Hamiltonian Neural Network, HNN）和端口哈密顿（port-Hamiltonian）思想，但中文正文应以“哈密顿”“端口哈密顿”等中文术语为主。重点不是展示术语覆盖面，而是说明：结构化神经动力学试图在可学习模型中保留可靠的几何、能量、耗散或功率关系。

### 5.5 第五段：提出 AUVHamNODE

推荐贡献列表如下：

1. 将本章问题界定为面向长期状态预测的 AUV 运动建模，并明确预测对象为位置、姿态、速度等运动状态；
2. 提出 AUVHamNODE，将 AUV 位姿运动几何、机械能量与耗散约束组织进连续时间神经动力学模型；
3. 在保留结构约束的同时，用可学习项描述难以精确参数化的非保守作用和受控输入影响；
4. 构建从黑箱模型、几何结构模型到能量结构模型和结构消融模型的评估链条；
5. 通过短时预测、长期递推、结构消融和物理诊断验证结构化建模对长期状态预测的作用。

---

## 6. 方法章不要按代码顺序写，而要按物理问题写

当前主稿 v2 已采用 8 节结构：研究问题与方法概述、相关建模基础、受控状态表示与海流速度约定、结构化连续时间动力学模型、能量平衡与理论边界、训练目标与验证协议、讨论、本章小结。以下物理逻辑不再作为一级目录，而是作为第 3--6 节内部段落展开顺序。

### 6.1 状态、控制与速度约定

先定义：

\[
y=[x,R,\nu_r,u_a,u_c,v_c^n,z_{ref}].
\]

其中 \(v_c^n\) 和 \(z_{ref}\) 是可选携带通道。注意不要把它们写成完整动力学子系统。更准确的说法是：

> 在单个积分窗口内，控制命令、海流向量和深度参考作为外源上下文随状态携带，其导数设为零。

### 6.2 SE(3) 运动学

写：

\[
\dot x=R(v_r+R^\top v_c^n),
\qquad
\dot R=R\hat\omega.
\]

解释：

- \(v_r\) 是相对水线速度；
- \(R^\top v_c^n\) 是体坐标系海流；
- \(v_r+R^\top v_c^n\) 是体坐标系总体线速度；
- 位置由总体速度推进；
- 角速度仍使用体坐标系角速度 \(\omega\)。

### 6.3 哈密顿量机械核心

写：

\[
p_r=M_\theta\nu_r,
\qquad
H_\theta(q,p_r)=\frac12p_r^\top M_\theta^{-1}p_r+V_\theta(q).
\]

其中 \(M_\theta^{-1}\succ0\) 通过 Cholesky 型参数化保证。势能 \(V_\theta(q)
\) 是标量函数，可包含姿态和可选深度上下文。

### 6.4 非保守力结构分解

写：

\[
f_\theta^{nc}
=-D_\theta(\xi)\nu_r
+J_\theta(\xi)\nu_r
+\tau_\theta(\nu_r,u_a,c),
\]

其中：

- \(D_\theta\succeq0\) 是耗散；
- \(J_\theta=-J_\theta^\top\) 是零功率横向/升力耦合；
- \(\tau_\theta\) 是可学习广义力分支；
- \(c\) 可以表示体坐标系海流或总体速度等可选上下文。

不要把 \(\tau_\theta\) 写成标准线性输入矩阵 \(G(q)u\)。更稳妥的表述是：

> 本文采用执行器状态驱动的可学习广义力分支，而非假设控制输入以固定仿射端口形式进入系统。

### 6.5 执行器滞后

写：

\[
\dot u_a=T_\theta^{-1}(u_c-u_a),
\qquad
\dot u_c=0.
\]

解释：

> 这样，控制命令不会被直接等同于广义力，模型需要通过一阶执行器状态和可学习广义力分支在每个 ODE 求解步计算实际作用。

### 6.6 能量平衡命题

建议使用以下命题形式：

> 命题：在静水或外源项固定、旋转矩阵位于 SO(3)、连续时间向量场精确、\(M_\theta\succ0\)、\(D_\theta\succeq0\)、\(J_\theta=-J_\theta^\top\)，且保守项由标量势能 \(V_\theta\) 生成的条件下，AUVHamNODE 的机械核心满足端口哈密顿风格的能量平衡：

\[
\dot H_\theta
=-\nu_r^\top D_\theta\nu_r+
u_r^\top\tau_\theta,
\]

或在有海流/外源上下文时写成：

\[
\dot H_\theta
=-\nu_r^\top D_\theta\nu_r+
u_r^\top\tau_\theta+P_{ext},
\]

其中 \(P_{ext}\) 表示由海流或外源上下文引入的额外功率项。

命题后必须加一句边界说明：

> 该命题刻画的是机械核心的结构性质，并不意味着完整的执行器—海流—神经广义力增强系统构成标准闭合端口哈密顿系统。

---

## 7. 实验章应写成“证据阶梯”

正式实验章不要只按“实验一、实验二、实验三”堆结果，而应按证据强度逐级推进。

### 7.1 第一层证据：短时控制块预测

要回答：模型能否拟合局部动力学？

建议报告：

- 位置 RMSE；
- 姿态 SO(3) 测地误差；
- 线速度误差；
- 角速度误差；
- 执行器状态误差；
- SO(3) 正交性和行列式诊断；
- ODE 求解失败率或无效预测率。

解释时要谨慎：短时误差低只能说明局部拟合能力，不足以证明长期稳定性。

### 7.2 第二层证据：长期 held-out rollout

要回答：结构是否抑制误差累积？

这是实验章核心。建议报告：

- completion rate；
- completed-to-horizon rate；
- final position error；
- final rotation geodesic；
- final total velocity error；
- failure reason；
- survival curve 或 completed time 分布。

解释重点：

> 如果 AUVHamNODE 的短时误差并非最低，但长期 completion rate、更低 p95 误差和更低失败率更好，这仍然是结构化模型的重要胜利。

### 7.3 第三层证据：海流速度变量关系验证

要回答：相对水速度约定是否必要？

建议比较：

- 无海流数据集与有海流数据集；
- 使用当前速度变量关系的模型与弱化/错误速度处理的模型；
- 总体速度误差和相对速度诊断的差异。

主 KPI 应看：

- 位置；
- 深度；
- 姿态；
- 总体速度。

相对速度误差应作为模型空间诊断，而不是主要可观测 KPI。

### 7.4 第四层证据：噪声初值鲁棒性

要回答：模型离开干净初值流形后是否稳定？

建议报告不同噪声 profile 下的：

- completion rate；
- 终端位置误差；
- 终端姿态误差；
- 总体速度违反率；
- SO(3) 违反率；
- failure reason 分布。

解释重点：

> 真实 AUV 递推预测不会总是从干净状态开始，导航误差、航向偏差、速度误差或海流估计偏差都会让模型进入训练分布边界。因此噪声初值鲁棒性是长期可用性的关键证据。

### 7.5 第五层证据：结构消融

要回答：每个物理结构是否真的有贡献？

建议至少包含以下模型或消融：

| 模型/消融 | 验证问题 |
|---|---|
| `blackbox_fullstate` | 完全黑箱是否容易长期漂移 |
| `se3_accel_blackbox` | 只保留 SE(3) 运动学是否足够 |
| `se3_momentum_blackbox` | 动量/质量结构是否有帮助 |
| `phnode_merged_force` | 合并非保守力是否弱于 D/J/B 分解 |
| `phnode_qforce` | 标量势能是否优于通用配置力 |
| `phnode_full` | 完整 AUVHamNODE 结构收益 |
| `ablate_no_mass_prior` | 质量先验是否改善收敛和泛化 |
| `ablate_diag_damping` | 耦合阻尼是否必要 |
| `ablate_no_lift` | 斜对称 lift 是否帮助横向/角向耦合 |
| `ablate_bu_only` | 执行器分支是否需要速度/海流条件 |

### 7.6 第六层证据：能量与 SO(3) 内部诊断

要回答：模型内部是否保持物理一致性？

建议报告：

- predicted energy span；
- max absolute energy delta；
- SO(3) determinant error；
- SO(3) orthogonality error；
- any SO(3) violation；
- 失败案例中的能量和 SO(3) 轨迹。

解释重点：

> 能量和 SO(3) 指标不是直接的外部任务误差，而是结构模型内部一致性的诊断证据。

### 7.7 第七层证据：数据效率和泛化

要回答：结构先验是否减少对数据量的依赖？

建议在不同训练数据比例下比较：

- 短时预测误差；
- 长期 completion rate；
- 终端位置和姿态误差；
- 失败率。

如果实验资源有限，可将数据效率实验放入附录或补充实验。

---

## 8. 结果解释时的优先级

正式论文中，结果解释建议按以下优先级：

1. **先看长期是否完成**：completion rate、completed-to-horizon rate、failure reason；
2. **再看完成轨迹的终端误差**：position、rotation、total velocity；
3. **再看误差增长过程**：median/p90 随时间增长；
4. **再看模型空间诊断**：relative velocity；
5. **最后看内部诊断**：energy、SO(3) raw quality。

这个顺序非常重要。不要先讲能量曲线再讲模型失败率，因为用户和审稿人首先关心的是长期预测是否能活到目标时长。

建议使用以下解释句式：

> 从短时控制块预测看，若干模型均能达到较低局部误差；但在长期递推中，完全黑箱模型和弱结构模型更容易出现误差累积、速度违反或求解失败。相比之下，AUVHamNODE 通过几何、能量和海流速度变量关系共同约束向量场，在较长预测时域内表现出更高完成率和更低尾部误差。该结果说明，结构约束的主要价值并不只体现在单步拟合精度，而更体现在长期递推稳定性和物理一致性上。

---

## 9. 推荐图表设计

至少建议做以下六张核心图。正式论文中，图的作用不是装饰，而是构建论证。

### 图 1：总体框架图

内容：数据块输入 → 数据态/模型态转换 → AUVHamNODE ODE → 短时预测和长期 rollout → 指标输出。

图中必须标出：

- 数据空间保存 \(\nu_b\)；
- 模型空间使用 \(\nu_r\)；
- ODE 状态包含 \(u_a,u_c,v_c^n,z_{ref}\)；
- 输出回到数据空间评估。

### 图 2：速度变量关系图

这是全文最重要的概念图。

建议画出：

\[
v_c^n \xrightarrow{R^\top} v_c^b,
\qquad
v_r+v_c^b=v_b,
\qquad
\dot x=Rv_b.
\]

同时标注：

- 水动力分支看 \(\nu_r\)；
- 位姿运动学看 \(\nu_b\)；
- 评估主 KPI 看数据空间总体速度。

### 图 3：机械核心功率分解图

画出：

- \(H_\theta\)：能量存储；
- \(D_\theta\)：耗散；
- \(J_\theta\)：零功率耦合；
- \(\tau_\theta\)：执行器广义力输入；
- \(u_c\to u_a\)：执行器滞后。

图中应明确写：

\[
\nu_r^\top J_\theta\nu_r=0.
\]

### 图 4：模型结构对比图

建议把模型排列成结构逐级增强的链条：

\[
\text{Full-State Black-Box}
\to
\text{SE(3) Accel}
\to
\text{SE(3) Momentum}
\to
\text{pH Merged/q-Force}
\to
\text{AUVHamNODE Full}.
\]

旁边标注每个模型保留/移除哪些结构。

### 图 5：长期 rollout 误差增长图

同一时域上画：

- position error median/p90；
- rotation geodesic median/p90；
- total velocity error median/p90。

最好按模型分组，而不是把所有曲线混在一起。

### 图 6：失败原因统计图

分开显示：

- completed；
- ground-truth divergence；
- prediction divergence；
- solver failure；
- NaN/Inf；
- depth violation；
- velocity violation。

这张图是长期预测论文的关键图，因为它能证明结构模型不是只在完成样本上误差低，而是整体更稳定。

可选图：

- 噪声初值鲁棒性箱线图；
- 能量 span 与终端误差散点图；
- SO(3) determinant/orthogonality 随时间变化；
- 不同数据量下的 completion rate；
- 典型成功和失败案例轨迹图。

---

## 10. 审稿风险控制表

建议在论文讨论章或附录中放一张类似下表的“理论声称范围表”。这不是示弱，而是主动降低审稿风险。

| 问题 | 可写结论 | 不应写结论 |
|---|---|---|
| 端口哈密顿性质 | AUVHamNODE 构造开放式端口哈密顿风格机械核心 | 完整 AUV—执行器—海流系统是严格标准端口哈密顿系统 |
| 能量平衡 | 静水或外源固定条件下，机械核心具有端口哈密顿风格能量平衡 | 任意海流、任意执行器输入下完整系统严格被动 |
| SO(3) 几何 | 连续时间向量场显式编码 SO(3) 运动学结构，并报告 SO(3) 诊断 | 普通 ODE 数值积分严格保持 SO(3) |
| 海流 | 海流作为外源携带通道，用于构造相对水速度和条件特征 | 海流被建模为闭合哈密顿环境子系统 |
| D/J/B 分解 | D/J/B 是物理诱导的结构化非保守力假设空间 | D/J/B 唯一恢复真实水动力机理 |
| 具体平台或仿真模型 | 提供数据、质量先验和受控验证环境 | 本文对具体平台模型完成完整符号级端口哈密顿化 |
| 实验泛化 | 仿真受控环境下验证结构收益 | 没有海试数据时声称真实海域泛化已被验证 |
| 长期稳定性 | 结构约束改善测试时域内的 completion rate 和误差增长 | 模型对任意长时域保证稳定 |

---

## 11. 可直接放进论文的关键段落

### 11.1 方法章开头段落

> 本文的核心思想是将 AUV 动力学中相对可靠的结构作为连续时间向量场的硬约束或结构化参数化，而将难以精确建模的水动力耦合和执行器广义力映射交给神经网络学习。具体而言，AUVHamNODE 同时维护三个层面的物理账本：第一，几何账本通过 SE(3) 运动学约束位姿演化；第二，能量账本通过正定质量、标量势能、正定耗散和斜对称零功率项刻画机械能流；第三，速度账本在数据空间和模型空间之间区分总体速度与相对水速度，使海流条件下的惯性运动和水动力作用保持一致。

### 11.2 速度约定段落

> 有海流时，航行器相对于惯性系的运动速度与其相对于周围水体的速度并不相同。前者决定位置运动学，后者决定水动力作用。为此，本文在数据集中保存体坐标系总体速度 \(\nu_b\)，而在模型内部使用相对水速度 \(\nu_r=\nu_b-[R^\top v_c^n;0]\)。预测输出返回数据空间时，再通过 \(\nu_b=\nu_r+[R^\top v_c^n;0]\) 恢复总体速度。这一数据—模型契约是海流场景下进行 AUV 动力学学习的基础。

### 11.3 能量结构段落

> AUVHamNODE 不是让神经网络直接学习任意六维加速度，而是在相对动量 \(p_r=M_\theta\nu_r\) 上定义机械存储函数 \(H_\theta(q,p_r)=\frac12p_r^\top M_\theta^{-1}p_r+V_\theta(q)\)。非保守广义力被进一步分解为正定耗散、斜对称零功率耦合和执行器驱动的可学习广义力分支。该设计使模型能够显式区分能量耗散、功率守恒耦合和外部输入功率，从而为长期递推提供物理诱导的结构约束。

### 11.4 实验章开头段落

> 实验评估的目标不是单纯比较一步预测误差，而是检验结构约束是否能在长期递推、海流扰动、噪声初值和物理诊断下提升模型可信度。为此，本文将实验组织为由短时控制块预测、长期 held-out rollout、海流速度变量关系验证、噪声初值鲁棒性、结构消融以及能量/SO(3) 诊断组成的证据阶梯。

### 11.5 讨论章边界段落

> 需要强调的是，AUVHamNODE 的端口哈密顿性质应理解为开放式机械核心的结构约束，而不是完整 AUV—执行器—海流闭环系统的严格标准端口哈密顿化。海流在当前实现中作为短积分窗口内的外源携带通道，执行器通过一阶滞后状态进入可学习广义力分支，普通 ODE 求解器也不构成严格李群积分器。因此，本文的理论主张限定于结构化向量场和机械核心能量平衡，而长期稳定性和物理一致性主要通过受控仿真、消融和诊断实验加以验证。

---

## 12. 正式写作顺序建议

当前主稿已进入正文骨架 v2 阶段，不需要再重开目录。后续写作应优先补强正文深度，建议按以下顺序推进。

### 第一步：补强受控状态表示与海流速度约定

原因：海流条件下的速度变量关系是后续模型定义的基础。如果这里不清楚，后面所有端口哈密顿、能量、海流和实验都会变得混乱。必须先写清楚 \(x,R,\nu_b,\nu_r,v_c^n,u_a,u_c\)、数据态和模型态转换、学习任务和评估任务。

### 第二步：补强结构化连续时间动力学模型

按以下顺序扩写：\(\mathrm{SE}(3)\) 运动学、哈密顿量机械核心、非保守力 D/J/\(\tau\) 分解、执行器滞后、海流和携带通道、相对动量动力学。该部分应按物理结构写，不按代码模块顺序写。

### 第三步：补强能量平衡与理论边界

能量命题必须包含条件、结论、证明和边界。尤其要区分机械核心的功率平衡、完整增强系统的受控开放系统属性，以及普通 ODE 数值积分下的 SO(3) 漂移风险。

### 第四步：补强训练目标与验证协议

先不写结果结论，只写数据来源、控制块预测、held-out rollout、指标分类、基线和消融、噪声 profile、失败条件和 completion rate。所有结果数值都应等 current evidence 主表导出后再进入正文。

### 第五步：补强讨论

讨论节要主动解释非闭合端口哈密顿、非严格李群积分、非唯一水动力辨识、仿真证据范围和真实海试泛化边界。

### 第六步：最后修订研究问题、摘要和本章小结

研究问题、摘要和小结应最后修订，因为它们要准确概括已经形成的理论、协议和证据边界。

---

## 13. 最终写作总纲

正式论文可以被概括为一句话：

> **AUVHamNODE 的贡献不是替代传统参数化模型，也不是把某一具体平台或工程仿真器逐项改写为严格闭合的哈密顿/端口哈密顿系统，而是提出面向长期状态预测的 AUV 结构化神经动力学建模方法：将位姿几何、能量与耗散约束以及可学习非保守作用组织为可训练、可消融、可长期递推验证的连续时间假设空间。**

全文所有章节都应服务于这句话。

可以把论文写成以下逻辑闭环：

1. AUV 运动建模难，是因为受控水下运动同时涉及几何结构、能量/耗散关系和难以完全参数化的非保守作用；
2. 黑箱 NODE 容易忽略这些结构；
3. AUVHamNODE 把这些结构写进连续时间向量场；
4. 相对水速度与总体速度关系解决海流下水动力和位姿运动的变量不一致问题；
5. D/J/\(\tau\) 分解让非保守力具有物理诱导结构；
6. 长期 rollout、失败率、噪声初值和消融实验验证结构不是装饰；
7. 理论边界和局限性说明该方法是可信的结构化学习框架，而不是过度声称的完整物理模型。

---

## 14. 开写前检查清单

正式动笔前请逐项确认：

- [ ] 是否统一使用“开放式端口哈密顿风格机械核心”？
- [ ] 是否避免把完整系统称为严格端口哈密顿系统？
- [ ] 是否在第 2 章清楚定义 \(\nu_b\)、\(\nu_r\)、\(v_c^n\)？
- [ ] 是否说明数据保存总体速度，而模型空间使用相对速度？
- [ ] 是否说明位置运动学由总体速度推进？
- [ ] 是否说明水动力/广义力分支由相对速度参数化？
- [ ] 是否区分可观测空间 KPI、模型空间诊断和内部物理诊断？
- [ ] 是否报告 completion rate 和 failure reason？
- [ ] 是否至少包含 `blackbox_fullstate`、`se3_accel_blackbox`、`se3_momentum_blackbox`、`phnode_merged_force`、`phnode_qforce`、`phnode_full` 等核心比较？
- [ ] 是否包含 `ablate_no_mass_prior`、`ablate_diag_damping`、`ablate_no_lift`、`ablate_bu_only` 等结构消融？
- [ ] 是否将能量诊断写成内部一致性证据，而不是直接观测 KPI？
- [ ] 是否把 SO(3) 几何写成“向量场结构 + 正则/诊断”，而不是“普通求解器严格保持”？
- [ ] 是否在讨论章主动承认海流、执行器、可辨识性、真实海试和李群积分器方面的局限？

---

## 15. 最终建议

如果只能保留一个写作原则，那就是：

> **不要把论文写成“我们实现了一个端口哈密顿神经 ODE 模型”；要把它写成“我们提出了一种面向长期状态预测的 AUV 结构化神经动力学建模方法，并用几何、能量与耗散约束、可学习非保守作用、消融和长期 rollout 证明这些结构有用”。**

这一写法更符合当前代码事实，也更容易通过专家审查。
