# 任务：修改 / 重写博士论文方法章 §1.8（实验结果与结构证据分析）与 §1.9（讨论）

## 角色
你是 AUV 运动建模与控制（Fossen 体系）、深度学习、neural ODE / 端口哈密顿结构化神经动力学交叉领域的资深专家，同时是中文学术论文写作高手。请以高水平期刊 / 优秀博士论文的标准工作。

## 目标文件与定位
- 主稿：`paper/drafts/auvhamnode_thesis_chapter_zh.tex`
- 本次范围：**§1.8「实验结果与结构证据分析」（`\label{sec:results-structure-evidence}`）** 与 **§1.9「讨论」（`\label{sec:discussion}`）**。§1.10「本章小结」可顺带核对一致性但不主动重写。
- 请按 `\section/\subsection` 标题定位，不要依赖固定行号。

## 本章背景（务必先通读全章再动）
- 本章提出 SE(3) 结构化端口哈密顿 Neural ODE（AUVHamNODE），面向海流环境下 AUV 运动状态的长期预测，与消融、黑箱基线对比。§1.1–§1.7 已建立：受控状态表示与**速度契约**（总体速度 vs 相对水速度）、SE(3) 结构化连续时间向量场、静水机械核心能量–功率性质、训练与验证协议（长期自由演化评估、结构消融、初值扰动、内部物理诊断）。
- §1.1–§1.7（连带 §1.8/§1.9 术语）刚完成三轮规范化（见审查报告 `paper/drafts/section_1_1to1_7_review_report_20260617_zh.md`），统一术语：**契约**（非"约定"）、**配置**（指 q=(x,R)，非"位形"）、**自由演化 / 长期预测**（非"递推/滚动"）、**黑箱**（非"黑盒"）。模型 M1 的全章正式名是「**端口哈密顿配置广义力基线**」。请新内容与这些术语保持一致。
- §1.8 现有结构：证据集说明（表 `tab:s8-evidence-protocol`）→ 几何结构与长期预测稳定性 → 能量核心对预测精度的作用 → 初值扰动条件下的鲁棒性与排序收敛 → 噪声训练协议的等价性 → 训练稳定性与无升力耦合消融的注记 → 内部结构诊断；表/图：`tab:s8-clean-main`、`fig:s8-two-level-evidence`、`tab:s8-noise`、`tab:s8-protocol`、`tab:s8-diag`。
- §1.9 现有结构：结构先验为何改善长期预测 / 与相关工作的关系 / 端口哈密顿语义的适用边界 / 连续时间结构与数值积分误差 / 非保守力的可辨识性与证据治理 / 仿真验证的范围与未来工作。

## 工作流程（强制）
1. **先讨论方案，待我确认后再执行**（作者与审稿分离：动稿前先与我确认范围）。
2. 第一步交付一份**中文方案**（用表格组织）：
   - 先单独给出一节「高水平 DL/NN/NODE 论文的实验与讨论部分通常如何组织」的提炼（结构惯例：如何陈述设置→主结果→消融→鲁棒性→诊断→机理解释→与相关工作对比→效度威胁/局限→未来工作；如何用图表承载结果、正文承载论证），作为后续组织的依据；
   - 再就**本章实际**，逐小节给出「保留 / 改写 / 重写 / 合并 / 新增」的判断 + 理由 + 新结构提纲；
   - 给出「修改 vs 重写」的总体建议与取舍；
   - 列出需要我拍板的开放问题。
3. 我确认后再动主稿；动稿后自行编译核验并派独立 agent 二次核验（见下）。

## 硬性要求（用户）
1. 这是长篇中文博士论文，**无篇幅限制**——该写清楚的地方写透，不要为压缩而牺牲清晰度与论证完整性。
2. **先想清楚高水平 DL/NN/NODE 论文的实验与讨论怎么写**，再结合本章实际合理组织，不要照搬现有段落顺序。
3. 这是**正式中文论文，不是备忘录 / 实验报告**：注意学术语言表达；**不要带入代码 / 实验报告式表达**——不出现源码变量名、函数名、数据集文件名（如 `oc_traj1000_blk150` 之类）、脚本名、配置键名；模型在正文中用中文描述名（如"完整模型""端口哈密顿配置广义力基线""全状态黑箱"），代号（M0/A1 等）即便保留也只宜出现在表格内，正文尽量少用。
4. **不要反复强调随机种子**：减少"五个种子""种子 43""某种子下表现""种子间均值/标准差""有效种子数 N"等表述；把这类可靠性记账下沉到表注或一句方法回指（§1.7），正文用干净的陈述（如"该基线在所有评估配置下均长期预测发散"），**正文不要逐一点名具体种子编号**。

## 补充要求（同样务必遵守）
5. **事实诚信 / 证据闸门**：§1.8 的每个数值、排名、"发散/收敛"判断都必须可追溯到证据目录，且尊重 `evidence_status`——
   - 数据来源：`analysis/oc_data_catalog/` 的 `canonical_rollout_*` 表；字段定义见 `docs/oc_data_catalog_dictionary.md`；选取规则见 `docs/oc_result_selection_policy.md`；引用任何行前先查 `evidence_status`，被判 `stale_environment_drift` 的运行不得进入主排序（如 `phnode_full clean seed42/46` 属 stale，用 cleanrun v1 基线，见 `docs/phase1a_oc_v4lite_cleanrun_v1_report.md`）。
   - **不得编造或"优化"数值**；若组织新内容时需要某个当前证据集没有的数字，**标记出来让我确认**，不要臆造。
   - **不得虚构未做过的实验**：例如步长 / 求解器阶数敏感性，§1.7 已明确声明"未单独考察"，§1.8/§1.9 不得声称做过此类实验或给出相关结论。
6. **沿用全章五条设计红线**（属有意为之，核查一致即可，切勿当作错误"修正"，更不得在新内容里破坏）：
   1. 端口哈密顿语义仅限开放式六自由度机械核心；τθ ≠ G(q)u；完整航行器–执行器–环境系统作为受控开放系统处理，不构成闭合端口哈密顿系统。
   2. 海流：模型契约层分段常值 / 数据生成层连续缓变 OU 外源场——两套口径各自准确、不可混为一谈。
   3. 普通 ODE 求解器**不严格**保持 SO(3) / 能量；不得出现"严格保群 / 严格保结构积分"之类过度声明（正交性误差小但非零，作诊断量）。
   4. 初值扰动 = 鲁棒性正则 / 对初值误差的敏感性侧面，**非导航后验**；噪声预算物理接地、与数据集无关。
   5. 性能数值与排名属于 §1.8（这是它们该出现的地方，红线 5 只约束 §1.7 本体）；但所有排名 / 结论必须**条件化**（"在当前模型族与数据协议下"），**不得外推为普遍必要性定理**；几何→稳定、能量核心→精度、质量先验与零功率升力耦合边际作用依次递减——这些是现有证据支持的判断，重写时应保留其科学内容与条件化口径，只改组织与表达。
7. **与 §1.1–§1.7 一致**：术语、记号、交叉引用（`\ref`/`\eqref` 指向正确）、语域统一；新写的机理解释应回指方法部分对应的引理/命题/式（如能量平衡命题、势能功率配对引理、SE(3) 运动学式）。
8. **可编译**：完成后用 `xelatex` 跑两遍，确保 0 Overfull、0 未定义引用、页数合理（当前整章 54 页；参考文献 `.bbl` 里有一条既存 Underfull 属正常，非你引入）。生成表格遵循现有 `tabularx` 风格，窄列勿塞长表头。

## 证据要点（供组织内容参考，仍以目录实查为准）
- 有效证据集覆盖 7 个模型：完整模型、无升力耦合消融、无质量先验消融、端口哈密顿配置广义力基线，以及全状态黑箱、SE(3) 加速度黑箱、SE(3) 动量黑箱；对角阻尼消融、广义力条件化消融、端口哈密顿合并广义力基线**未纳入**本章统一重建证据集，不作定量结论。
- 两级核心结论：① SE(3) 位姿几何是长期自由演化稳定的关键结构因素（全状态黑箱在所有评估配置下发散，保留 SE(3) 结构者维持有限误差）；② 在保持几何与质量结构前提下，机械能量核心是长期精度的主要结构先验（完整模型最优，移除标量势能的配置广义力基线退化为最弱结构化对照）。另有：噪声初值训练协议的等价性、初值扰动下的排序收敛、内部诊断与结构解释一致。
- 证据治理是诚信要点（须保留但表达学术化）：统一代码环境 / 数据镜像下重建，环境漂移或被替代的历史运行只用于说明溯源边界、不进入主排序。

## 工作方法与交付
- 判断前先**完整通读**相关章节与证据目录，**独立核验事实**（数值对目录、`\label` 存在性、口径），不要轻信现有注释或既有结论。
- 全程**用中文回答、用表格组织**关键信息。
- 动稿后：自行 `xelatex` 编译核验，并**派一个独立 agent 做二次核验**（科学内容/数值可追溯、五红线无损、交叉引用闭环、术语与 §1.1–§1.7 一致、语域达标、无代码/种子腔），把核验结论回报给我。
- 提交策略：在非 main 分支提交，commit message 用中文、`docs:` 前缀；本地不主动推送，等我指示。

先给我**方案讨论**（含"高水平论文怎么写"提炼 + 逐小节处置建议 + 修改/重写总体建议 + 待我拍板的问题），不要直接改稿。
