# Legacy Phase-1 Implementation Checklist

This document archives the old Phase-1 implementation-checklist entry.

The active Phase-1 implementation plan is:

- [../phase1_realistic_validation_plan.md](../phase1_realistic_validation_plan.md)

The previous checklist assumed the full Phase-1 matrix should be implemented directly. That approach is no longer active. The current implementation route is:

1. implement and validate `v4-lite` only as a protocol-sensitivity check;
2. run the lightweight `Phase-1A` model-evidence decision package;
3. enter `Phase-1B` only if model conclusions change or attribution remains unclear.

This archive is kept only to explain why the old file path disappeared from the active docs.
